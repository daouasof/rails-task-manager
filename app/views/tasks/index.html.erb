<h1>List of the tasks : </h1>

<ul>
<% @tasks.each do |task| %>
  <li>
  <% if task.completed %>
  [x]
  <% else %>
  [ ]
  <% end %>
  <%= link_to task.title, task_path(task.id) %></li>
<% end %>
</ul>

<%= link_to "Add a new task", new_path %>
